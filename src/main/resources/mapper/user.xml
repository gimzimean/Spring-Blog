<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cos.blog.repository.UserRepository">

	<select id="findAll" resultType="com.cos.blog.model.user.User"><!-- list 타입이라고 안해도 됨 -->
		select * from mem order by id desc
	</select>
	
	<insert id="save">
	insert into user(username, password, email, createDate) 
	values (#{username},#{password},#{email},now())
	</insert>
	
	
	<select id="findByUsername" resultType="int"><!-- list 타입이라고 안해도 됨 -->
		select count(*) from user where username=#{username}
	</select>
	
	
	<select id="findByUsernameAndPassword" resultType="com.cos.blog.model.user.User"><!-- list 타입이라고 안해도 됨 -->
		select id, username, email, profile from user where username=#{username} and password=#{password}
	</select>

	
</mapper>